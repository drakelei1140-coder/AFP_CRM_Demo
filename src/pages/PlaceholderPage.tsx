import { Card, Typography } from 'antd';

export const PlaceholderPage = () => (
  <Card>
    <Typography.Title level={4}>功能预留</Typography.Title>
    <Typography.Text type="secondary">本次任务范围外页面，仅保留菜单入口与布局一致性。</Typography.Text>
  </Card>
);
